[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA — React/Vite (evita 404 ao abrir subcaminhos)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
